---
globs: "src/components/**/*.tsx;src/components/**/*.jsx"
---
Always wrap default exports with React.memo().
Inline styles are forbidden — use CSS modules instead.
Event handlers should be prefixed with `handle`: `handleClick`, `handleSubmit`.
