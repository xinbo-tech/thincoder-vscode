---
globs: "*.css;*.scss"
description: "CSS conventions for this project"
---
Use CSS custom properties (variables) for all colors.
Avoid `!important`. Use specificity instead.
Prefer `rem` over `px` for font sizes.
